int[] numbers = {10, 20, 30, 40, 50}; // Creating an integer array

int firstNumber = numbers[0]; // Accessing the first element (index 0)
int thirdNumber = numbers[2]; // Accessing the third element (index 2)

int arrayLength = numbers.length; // Getting the length of the array (number of elements)
